# Examples

These examples use placeholder paths. Replace `path/to/...` with paths for the Gaussian output file and the molecular-orbital cube directory in the local environment.

Use `run_contribution_map_example.bat` for signed contribution maps. Use `run_validation_example.bat` to test coefficient-linear EDTM and MDTM reconstruction against Gaussian transition-moment tables.
